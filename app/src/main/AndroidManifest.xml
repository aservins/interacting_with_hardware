<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.exodoestudios.hardware">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DiceActivity"></activity>
        <activity android:name=".LevelActivity" />
        <activity android:name=".GestureActivity" />
        <activity android:name=".ProximityActivity" />
        <activity android:name=".MapActivity" />
        <activity android:name=".CameraActivity" />
        <activity android:name=".StepCounterActivity" />
        <activity android:name=".GiroscopeActivity" />
        <activity android:name=".AcelerometerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBK2YPcXQnDExMww3AtOYwu7kNPXD0FDPk" />
    </application>

</manifest>